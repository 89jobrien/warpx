name: Protect main branch

on:
  pull_request:
    branches: [main]

jobs:
  block:
    runs-on: ubuntu-latest
    steps:
      - name: Reject non-upstream PRs to main
        run: |
          echo "::error::main is upstream-only. Target develop for custom work."
          exit 1
