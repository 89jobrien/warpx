paths-ignore:
  # Binary ML model — byte sequences incidentally match secret patterns.
  - "crates/input_classifier/models/fasttext/cmd_lang_classifier_v4.bin"
  # SQLite test fixture — contains snapshot data with redacted/fake credentials.
  - "crates/integration/tests/data/cloud_objects.sqlite"
  # Secret detection test fixtures — intentionally contain fake/test keys
  # to verify the redaction and blocklist pipelines.
  - "app/src/terminal/model/secrets_test.rs"
  - "app/src/ai/blocklist/block/secret_redaction_test.rs"
  # Channel config contains a fake Google API key used in unit tests.
  - "crates/warp_core/src/channel/config.rs"
